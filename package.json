{
  "name": "flexzone-workspace",
  "version": "1.0.0",
  "description": "FlexZone Gym Management System Workspace Launcher",
  "scripts": {
    "install-all": "cd frontend && npm install",
    "backend": "cd backend && mvnw spring-boot:run",
    "frontend": "cd frontend && npm start",
    "start": "npx -y concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run backend\" \"npm run frontend\""
  }
}
